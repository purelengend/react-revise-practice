import { Text } from "@chakra-ui/react";

const Fallback = () => {
  return (
    <Text textAlign="center" fontSize="2xl" color="red">
      Something went wrong.
    </Text>
  );
};

export default Fallback;
